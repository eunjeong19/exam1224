{{> layout/top}}

<style>
    .add{
        text-decoration:none;
        transition: 0.2s;
    }
    .add:hover{
        text-decoration:underline;
        color: #198754;
    }
    .form-control:read-only{
        background-color:#e9ecef;
        cursor:not-allowed;
    }
</style>

<div class="container my-5 py-5" style="max-width: 900px;">
    <div class="card shadow-sm border-0 p-4 p-md-5 bg-light">
        <div class="card-body">

            <div class="text-center mb-5">
                <div class="badge bg-success-subtle text-success mb-2">Edit Mode</div>
                <h1 class="fw-bold"> 회원 정보 수정 </h1>
                <p class="text-muted">정보 수정 후 완료 버튼을 눌러주세요</p>
                <hr class="my-4 mx-auto" style="width: 80px; border-top: 3px solid #198754;">
            </div>

            <form method="post" action="/member/memberModify">

                <div class="row g-1 align-items-center mb-4 justify-content-center">
                    <div class="col-2 text-end px-2">
                        <label class="col-form-label fw-bold">아이디</label>
                    </div>
                    <div class="col-md-5">
                        <input type="text" class="form-control" value="{{member.id}}" readonly>
                        <input type="hidden" name="id" value="{{member.id}}">
                    </div>
                    <div class="col-3 ps-3">
                        <span class="form-text small text-danger">ID는 변경 불가</span>
                    </div>
                </div>

                    <div class="row g-1 align-items-center mb-4 justify-content-center">
                        <div class="col-2 text-end px-2">
                            <label class="col-form-label fw-bold">비밀번호</label>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="password" class="form-control" value="{{member.password}}">
                        </div>
                        <div class="col-3 ps-3">
                            <span class="form-text small">8-20자 이내</span>
                        </div>
                    </div>

                    <div class="row g-1 align-items-center mb-4 justify-content-center">
                        <div class="col-2 text-end px-2">
                            <label class="col-form-label fw-bold">이름</label>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="name" class="form-control" value="{{member.name}}">
                        </div>
                        <div class="col-3 ps-3"></div>
                    </div>

                <div class="row g-1 align-items-center mb-4 justify-content-center">
                    <div class="col-2 text-end px-2">
                        <label class="col-form-label fw-bold">나이</label>
                    </div>
                    <div class="col-md-5">
                        <input  type="number" name="age" class="form-control" value="{{member.age}}">
                    </div>
                    <div class="col-3 ps-3"></div>
                </div>

                <div class="row g-1 align-items-center mb-4 justify-content-center">
                    <div class="col-2 text-end px-2">
                        <label class="col-form-label fw-bold">지역</label>
                    </div>
                    <div class="col-md-5">
                        <input type="text" name="addr" class="form-control" value="{{member.addr}}">
                    </div>
                    <div class="col-3 ps-3"></div>
                </div>

                <div class="row g-1 align-items-center mb-4 justify-content-center">
                    <div class="col-2 text-end px-2">
                        <label class="col-form-label fw-bold">연락처</label>
                    </div>
                    <div class="col-md-5">
                        <input type="text" name="mobile" class="form-control" value="{{member.mobile}}">
                    </div>
                    <div class="col-3 ps-3"></div>
                </div>

                <div class="text-center mt-5">
                    <button type="submit" class="btn btn-success btn-lg px-5 shadow-sm fw-bold">
                        정보 수정 완료
                    </button>
                    <div class="mt-3">
                        <a href="/member/memberInfo/{{member.id}}" class="add small text-secondary">
                            수정 취소하고 돌아가기
                        </a>
                    </div>
                </div>
                </form>
        </div>
    </div>
</div>

{{> layout/bottom}}